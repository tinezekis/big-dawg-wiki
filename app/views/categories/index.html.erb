<div class="row">
	<div class="col s7 offset-s3">
		<h1 class="white-text">All Categories</h1>
	</div>
</div>

<% @categories.each do |category| %>
	<div class="divider"></div>
		<div class="section deep-purple darken-2">
	    <h3><%= link_to category.name, category_name_path(category.slug), class: "index_li white-text" %></h3>
		</div>
<% end %>
