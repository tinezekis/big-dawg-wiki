<div class="row">
	<div class="col s10 offset-s1">
		<h1 class="brown-text text-darken-2">New Version For <%= @version.article.title %></h1>
	</div>
</div>

<div class="row">
	<form id="create-entry-form" action="/articles/<%= @version.article.to_param %>" method="POST">
	  <textarea name="version[content]" row="5" cols="50" placeholder="Enter Content"></textarea>
	  <input type="text" name="version[footnotes]" placeholder="Enter Footnotes">
	  <input type="text" name="categories" placeholder="Categories (separate by commas)">
	  <button class="btn waves-effect green darken-1 waves-light" type="submit" name="action">Create New Version
	     <i class="material-icons">send</i>
	   </button>
	</form>
</div>
