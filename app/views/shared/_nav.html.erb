<div class="navbar-fixed">
  <nav class="green darken-1">
    <div class="nav-wrapper">
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <% if current_user %>
          <li>
            <form id='search-bar' action="/articles/search" method='get'>
              <div class="input-field">
                <input id="search" name="term" type="text" required>
                <label for="search"><i class="material-icons">search</i></label>
              </div>
            </form>
          </li>
          <% if current_user.permission_level == "big_dawg" %>
            <li><%= link_to "All dawgs", users_path %></li>
          <% end %>
          <li><%= current_user.username %></li>
          <li><%= link_to 'Home', '/articles/you_should_read' %></li>
          <li><%= link_to 'Create new article', new_article_path %></li>
          <li><%= link_to 'Logout', :logout, method: :delete %></li>
      <% else %>
        <li>Login or sign up Below!</li>
      <% end %>
      </ul>
    </div>
  </nav>
</div>

